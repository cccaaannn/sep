server.port: 8090

spring:
  profiles.active: dev
  application.name: sep-discovery-service
  main.allow-circular-references: true

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: false
  instance.lease-renewal-interval-in-seconds: 0
  dashboard.path: /eureka-dashboard
